# 變更紀錄

本檔案記錄本專案所有重要變更，格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版號遵循 [語意化版本](https://semver.org/lang/zh-TW/)。章節標頭沿用 Keep a Changelog 的英文標準：`Added`、`Changed`、`Deprecated`、`Removed`、`Fixed`、`Security`。

## [1.1.0] - 2026-07-25

### Added

- 中文大寫九格中、高於最高有效位而沒有大寫數字的空格，現在會依紙本習慣以橫線槓掉：總計有值時，這些空格會以一條橫貫該格、同 olive 印刷墨色的橫線劃掉（防止事後塗改加大金額），印刷單位字仍維持可見；總計為 0 或空白時九格一律不劃線。

### Changed

- 金額欄位改為顯示千分位，便於核對：各列金額、單價、銷售額、營業稅額、總計，以及二聯式框外的內含稅額／銷售額，離開聚焦後顯示如 `10,500`；欄位聚焦編輯中仍顯示純數字，避免逗號打斷輸入或讓游標亂跳。這些欄位改用 `type="text"` 搭配對應的 `inputmode`（整數金額 `numeric`、單價 `decimal`），手機仍會跳出數字鍵盤。解析輸入時會先剝除千分位逗號再正規化，貼上「10,500」可正確解析成 `10500`；單價允許小數，千分位只加在整數部分。數量、期別年份、民國日期年月日不受影響。
- 測試檔集中管理：單元與元件測試移至 `tests/unit/`、端對端測試移至 `tests/e2e/`（原本分散在 `src/` 與 `e2e/`）。`tests/unit` 已納入 `vue-tsc` 型別檢查範圍。

### Fixed

- 民國日期不再能填出不存在的日期（例如 8 月 32 日）：月只接受 1～12（超出夾到 12），日只接受 1～該月天數（超出夾到上限，天數依民國年換算西元年判斷閏年，二月 28 或 29 日，月份未填時上限以 31 計）；改月份或年份後，若既有的日已超出新上限會自動跟著夾（例：日 31、月改 2 → 28 或 29；日 29、年由閏年改平年 → 28），並同步回寫畫面。
- 修正千分位調整引入的迴歸：金額欄 focus 時把顯示值由千分位（如「12,345」）同步回寫成純數字（「12345」）會把聚焦當下已建立的全選範圍清掉、游標收合到字尾，導致鍵盤 Tab 進入欄位後直接打字變成「附加」而非「取代」（12,345 → Tab → 打 500 → 誤變成 12345500）。三個金額 focus handler（列金額／單價／銷售額稅額總計）現在回寫後一律把選取範圍設回全選，維持「取代」語意。
- 單價欄（`item-price-N`）改用 `type="text"` 後失去瀏覽器數字過濾、可輸入「1a2.5b」等含字母字串：編輯中已即時濾除逗號以外的非數字雜訊字元（僅保留數字與最多一個小數點，並保留使用者打到一半的小數點如「12.」），避免此類輸入被判為 `null`、金額歸零，並在 blur 時未經提示就靜默清空整格。

### Notes

- 測試：160 個單元與元件測試（Vitest）、29 個端對端測試（Playwright），型別檢查與 production build 皆通過。
- 上述兩條 Fixed 的迴歸測試均經反向驗證：暫時移除修正後測試確實轉紅，還原後全綠。

## [1.0.0] - 2026-07-25

首次發布。台灣手寫統一發票的開立輔助工具：畫面與紙本發票同規格，發票上可書寫的欄位皆可自行填寫，自動帶入稅額拆分與金額中文大寫。

### Added

**發票版面**

- 三聯式／二聯式切換，版面依紙本統一發票規格呈現：標題、期別、買受人／統一編號／地址、置中的民國開立日期、五欄表格（品名、數量、單價、金額、備註）、固定五列品項、營業人蓋用統一發票專用章區、銷售額合計、營業稅（應稅／零稅率／免稅）、總計、九格中文大寫欄、註腳與聯次標示。
- 二聯式省略統一編號列，金額區只留總計列與中文大寫列（無銷售額合計、無營業稅列），並於發票框外顯示內含稅額與銷售額。
- 品項固定五列、無增刪按鈕，與紙本一致；備註欄僅前兩列可填，第三列為「營業人蓋用統一發票專用章」印刷字，第四列起為合併的蓋章空白區。
- 紙本配色（米黃紙張、橄欖綠印刷線）、外框 2px 較內線 1px 粗、印刷標籤分散對齊撐滿儲存格（「零稅率」刻意連成一組置中）。
- 窄螢幕以橫向捲動呈現（發票最小寬度 640px），表格比例不重排；僅民國日期行在 700px 以下獨立成行，避免統一編號八碼被裁切。

**稅額計算**

- 雙向計算：填未稅銷售額往下推算稅額與總計，或填含稅總計反推銷售額與稅額。
- 恆等保證 `銷售額 + 稅額 = 總計`，含稅反推以減法求稅額，發票上不會出現一元尾差。
- 稅制切換：應稅 5%、零稅率、免稅，勾選後即以現有銷售額重算；切換至二聯式時稅制回到應稅。
- 金額欄一律正規化為非負整數元（負數、小數、非數字皆安全處理）；數量與單價可填小數，供秤重或單價含角的情形使用。
- 稅額尾數依《加值型及非加值型營業稅法施行細則》第 32-1 條四捨五入。

**欄位彈性**

- 發票上可書寫的欄位皆可自行填寫，包含各列金額、銷售額、營業稅額、總計等衍生欄位；手動覆寫某列金額後，修改該列數量或單價會恢復自動計算。
- 品項合計自動帶入金額區：三聯式視為未稅、二聯式視為含稅；合計為 0 時不覆蓋已填金額。
- 期別年份可填、月份為六個雙月期下拉；民國開立日期的年、月、日三格皆可填。年份與期別預設為系統日期所屬的民國年與當期雙月期。
- 日期與期別年份只接受阿拉伯數字，非數字字元即時濾除。
- 品名與備註可換行，長字串不會被截斷；視窗寬度或字級變化時會重新計算高度。
- 金額為 0 時依紙本留白，但明確在某列金額填 0（如贈品列）會顯示 0。
- 一鍵清除重填：買受人、品項、金額與稅制歸零，民國日期的月、日清空，年與期別回到當年當期預設值（發票類型保持不變）。

**金額中文大寫**

- 自動跟隨總計，九格對應億、仟、佰、拾、萬、仟、佰、拾、元。
- 數字以手寫墨藍色標示、字級略大，與橄欖綠印刷單位字區隔，並貼齊各自的位數單位（與紙本寫法一致）。
- 支援一至九位數；總計為 0 或超過九位數時九格留白，不會顯示錯位數字。

**無障礙**

- 發票類型分頁可用 Tab 聚焦、Enter 或空白鍵切換。
- 稅制三格為可聚焦的按鈕並帶 `aria-pressed` 狀態。

### Notes

- 純前端，不需要後端；不使用 localStorage、cookie 或任何網路請求，不儲存任何資料。
- 品名與備註的自動增高採用 ResizeObserver 搭配 requestAnimationFrame 延後寫入與冪等的高度計算，避免 observer 迴圈警告；並有端對端測試守門，確保操作全程不產生瀏覽器 console 錯誤。
- 測試：115 個單元與元件測試（Vitest）、23 個端對端測試（Playwright），涵蓋恆等式全值域掃描、負數與小數輸入、長字串不截斷、視窗與字級變化重算、外框線寬、console 無錯誤等迴歸案例。
- 文件：`docs/PRD.md`（產品需求）、`docs/ui-spec.md`（版面與互動的權威規格，含刻意為貼合紙本而保留的行為）。

### Known Issues

- 二聯式仍沿用三聯式的註腳文字與「第一聯　存根聯」標示。
- 中文大寫僅以九格形式呈現，未提供整串文字（如「壹萬零伍佰元整」）的輸出。
- 僅支援一般應稅 5%、零稅率與免稅，不處理特種稅額。
- 依「所有欄位可自行填寫」的設計原則，不驗證統一編號檢查碼與日期合法性（日期合法性驗證已於 1.1.0 修正）。

[1.1.0]: https://github.com/Sammy-Y/hand-write-receipt/releases/tag/v1.1.0
[1.0.0]: https://github.com/Sammy-Y/hand-write-receipt/releases/tag/v1.0.0
