# 手寫發票小幫手

台灣手寫統一發票的開立輔助工具。畫面與紙本發票同規格，使用者照著螢幕抄到紙上。
純前端、不儲存任何資料、已部署於 https://hand-write-receipt.vercel.app

## 硬性約束（違反等於做錯）

1. **畫面必須跟紙本統一發票一樣，不得自創版面**。參考圖：`docs/assets/invoice-reference.png`
2. **發票上可書寫的欄位都要能自己填**，包含衍生欄位（各列金額、銷售額、營業稅額、總計）
   可手動覆寫。自動計算只是輔助帶入。這是本專案存在的理由。
3. **`docs/ui-spec.md` 是版面與互動的權威規格**。它與 `docs/PRD.md` 或直覺衝突時，
   以 ui-spec 與實作為準。改 UI 前先讀它。

## 刻意保留的行為（不要「順手修正」）

- 中文大寫數值貼齊各自的印刷單位字，位數少時整串會偏右、左側單位字留空 → 紙本寫法
- 空的單位字**不隱藏**，改以橫線槓掉（防塗改），且總計為 0 時不劃線
- 金額 0 依紙本留白；但列金額被明確填 0（贈品列）要顯示 0
- 金額欄離開聚焦顯示千分位、聚焦編輯中顯示純數字（避免逗號打斷輸入）
- 品項固定五列、無新增／刪除按鈕；備註欄只有前兩列可填
- 窄螢幕橫向捲動、版面不重排（唯一例外：民國日期行在 700px 以下獨立成行）
- `tests/e2e` 刻意不納入 `vue-tsc` 型別檢查（需要 node 型別環境），`tests/unit` 有納入

## 指令

- `npm run dev` — 開發伺服器
- `npm run build` — 型別檢查（`vue-tsc -b`）＋ production build
- `npm test` — 單元／元件測試（Vitest）
- `npm run test:e2e` — 端對端測試（Playwright）

## 結構

- `src/App.vue` — 狀態中樞與所有計算連動
- `src/components/InvoiceSheet.vue` — 整張發票的版面與欄位（scoped CSS 紙本樣式）
- `src/lib/invoice.ts` — 純函式：稅額拆分、金額正規化與格式化、中文大寫、日期夾值
- `tests/unit/`（Vitest）、`tests/e2e/`（Playwright）

計算邏輯一律收在 `src/lib/invoice.ts` 的純函式，元件只負責呈現與事件。
所有寫入金額的路徑都必須經過 `normalizeMoney()`，否則會出現 `47.99000000000001`
這類浮點雜訊，且非整數會讓中文大寫九格整排空白。

## 測試規範

- **不得為了讓測試通過而放寬斷言、或在測試裡加繞道迴避應用程式的 bug。**
  （歷史事故：千分位改動造成 Tab 進欄位打字變成「附加」而非「取代」，當時的處理是在
  e2e 的 `fill()` 前插 `focus()` 繞過，並把成因誤植給 Playwright。那是應用程式 bug。）
- 修 bug 要補迴歸測試，並反向驗證它有效：暫時移除修正 → 測試必須轉紅 → 還原。
- 斷言金額請比對實際字串（如 `10,500`），不要只比數字或用寬鬆正規式。

## 工作流程

- **不要自動 commit 或 push**。`main` 已接 Vercel 自動部署，push 就是上線。
- Vercel 只跑 `npm run build`（型別錯誤會擋下部署），**不跑測試**。測試要自己跑。
- 發布流程見 `.claude/skills/release/SKILL.md`（或 `/release`）。
